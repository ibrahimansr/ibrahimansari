/**
 * The three rows from legacy `app/about` (“Other”) before it was removed.
 * Indices refer to `TRACKS` in `./tracks`.
 */
/** cold = Cold by Nemzzz (TRACKS[3]); 2Tone; Fly Me */
export const LEGACY_THREE_TRACK_INDICES = [3, 0, 5] as const;

export const LEGACY_THREE_LABELS = [
  'cold — nemzzz',
  '2tone — yeat, don toliver',
  'fly me to the moon — frank sinatra',
] as const;
